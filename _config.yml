url: "https://team-wurstwasser.com"
plugins:
  - jekyll-sitemap
defaults:
  - scope:
      path: "impressum.html"
    values:
      sitemap: false
  - scope:
      path: "mitglieder"
    values:
      sitemap: false